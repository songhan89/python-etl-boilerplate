import pandas as pd
import numpy as np
from typing import Dict, Any
from config.config import PIPELINE_CONFIG
from utils.logger import setup_logger

# Get logger
log = setup_logger()

def parse_data(var: str, cfg: Dict[str, Any]) -> pd.DataFrame:
    pass

